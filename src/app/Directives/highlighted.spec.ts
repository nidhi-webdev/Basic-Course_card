import { Highlighted } from './highlighted';

describe('Highlighted', () => {
  it('should create an instance', () => {
    const directive = new Highlighted();
    expect(directive).toBeTruthy();
  });
});
